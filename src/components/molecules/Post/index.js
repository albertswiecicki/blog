import React from "react";

// it will pull data from store <except the image ofc., image will by downloaded based on url>
const Post = ({ postId }) => {
  return <div>post with id {postId} here :)</div>;
};

export default Post;
